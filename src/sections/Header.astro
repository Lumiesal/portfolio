---

---
<script>
    // Seleccionar el header
    const theHeader = document.getElementById('header');
    const theHome = document.getElementById('home');

    // Manejar el evento de scroll
    window.addEventListener('scroll', () => {
      // Verificar la posición de scroll
      if (window.scrollY > 50) {
        // Agregar la clase 'fijado' al header cuando se hace scroll hacia abajo
        theHeader?.classList.add('fijado');
        theHome?.classList.add('fijado');
      } else {
        // Quitar la clase 'fijado' del header cuando se vuelve a la parte superior
        theHeader?.classList.remove('fijado');
        theHome?.classList.remove('fijado');
      }
    });
</script>

<header id="header" class="fixed top-5 left-0 right-0 flex w-[98%] max-w-8xl m-auto p-3 bg-custom-gray rounded-[20px] lg:rounded-full z-[99]">
    <nav id="nav" class="relative flex justify-center items-center w-full">
        <div class="logo lg:absolute lg:top-0 lg:bottom-0 lg:left-0 lg:right-0 flex justify-center align-middle w-max mr-auto lg:m-auto">
            <img src="logo-lumiesal.svg" alt="Icono" class="block m-auto">
        </div>
        <ul class="nav-links w-full hidden lg:flex">
            <li><a class="bg-custom-green" href="#home">Inicio</a></li>
            <li><a class="text-white" href="#projects">Proyectos</a></li>
            <li class="mr-auto"><a href="#about">Resumen</a></li>
            <li class="ml-auto"><a href="#skill">Habilidades</a></li>
            <li><a href="#contact">Contacto</a></li>
            <li><a href="#blog">Blog</a></li>
        </ul>
        <div class="block lg:hidden">
          <button id="hamburger" title="Open menu movil" class="text-white focus:outline-none">
            <svg class="size-8" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path class="fill-white text-white" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
        </button>
      </div>
    </nav>
</header>